{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install --legacy-peer-deps",
  "framework": null,
  "regions": ["iad1"],
  "functions": {
    "frontend/app/**/*.{js,ts,tsx}": {
      "maxDuration": 10
    }
  }
}
